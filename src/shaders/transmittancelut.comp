#version 450
layout(local_size_x = 8, local_size_y = 8, local_size_z = 1) in;

layout(rgba32f, set = 0, binding = 0) uniform image2D transmittance;
layout(set = 1, binding = 0) uniform UBO {
	vec3 sunDir;
} ubo;

void main() {
	ivec2 coord = ivec2(gl_GlobalInvocationID.xy);
}